# Optional MiOpIIk 0.2 package: no longer mounted by the default meta/preset.
# Install this bundle explicitly when learn behavior is desired.
- insert:
    - id: dsh-miopiik-learn
      name: 'dsh-miopiik-learn'
